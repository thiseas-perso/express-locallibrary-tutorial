extends layout

block content
  h1 #{id} 
  p #[strong Title: ] #{title} 

  p Do you really want to delete this Book Instance?

  form(method='POST' action='')
    div.form-group
     input#bookinstanceid.form-control(type='hidden',name='bookinstanceid', required='true', value=id )

    button.btn.btn-primary(type='submit') Delete